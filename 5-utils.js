const sayHi = (name) => {
    console.log(`Hello there ${name}`);
};

module.exports = sayHi

// this fairly simply exports the function
// note that this is functionally the same as ES6 syntax of export default (and then what you are exporting)